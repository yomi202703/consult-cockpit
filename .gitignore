# Secrets — the Gemma endpoint credentials. Shippable internally, never committed.
.env
.env.local
*.key

# Python (regenerable)
__pycache__/
*.py[cod]

# macOS
.DS_Store

# ivy workspace(.ivy/)— view 本体(*.html)はコミットする(恒久的な閲覧面)
.ivy/*.seen.html
.ivy/*.layout.json
.ivy/attachments/
.ivy/.watch-snap
.ivy/agent-chat.md
.ivy/serve.log
